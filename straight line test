// this is the initial component testing for the robot moving in a straight line

#include <stdio.h>
#include <time.h>
#include “E101.h”

int main(){
	init();
	set_motor(1, 127);
	set_motor(2, 127);
	sleep1(1,000000);
	set_motor(1, 0);
	set_motor(2, 0);

return 0;
	}
